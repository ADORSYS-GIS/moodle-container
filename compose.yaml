services:
  moodle-init:
    build: &moodle_build
      context: .
      dockerfile: Dockerfile
    image: moodle-nginx:latest
    environment: &moodle_env
      SITE_URL: http://localhost:8080
      MOODLE_LANGUAGE: en
      MOODLE_SITENAME: "Local Development Moodle"
      MOODLE_SITESUMMARY: "Moodle development instance powered by Nginx and Redis"
      MOODLE_USERNAME: admin
      MOODLE_PASSWORD: MoodleAdmin123!
      MOODLE_EMAIL: admin@local.moodle
      DB_TYPE: mariadb
      DB_HOST: mariadb
      DB_HOST_PORT: 3306
      DB_NAME: moodle
      DB_USER: moodle_user
      DB_PASS: moodle_pass
      DB_PREFIX: mdl_
      REDIS_SESSION_ID_HOST: redis
      REDIS_SESSION_ID_PORT: 6379
      REDIS_SESSION_ID_AUTH_STRING: "moodledevpass"
      REDIS_SESSION_IP_AND_PORT: redis:6379
      REDIS_SESSION_AUTH_STRING: "moodledevpass"
      REDIS_APP_IP_AND_PORT: redis:6379
      REDIS_APP_AUTH_STRING: "moodledevpass"
      REDIS_LOCK_HOST_AND_PORT: redis:6379
      REDIS_LOCK_AUTH_STRING: "moodledevpass"
      ENABLE_FRESHCLAM: "no"
      ENABLE_MOOSH_BOOTSTRAP: "no"
    entrypoint:
      - /opt/setup_moodle.sh
    volumes:
      - moodledata_data:/moodleroot/moodledata
    depends_on:
      mariadb:
        condition: service_healthy
      redis:
        condition: service_healthy
      # plugin-downloader:
      #   condition: service_completed_successfully

  moodle:
    build: *moodle_build
    image: moodle-nginx:latest
    ports:
      - "8080:8080"
    environment: *moodle_env
    volumes:
      - moodledata_data:/moodleroot/moodledata
      # - plugins_downloads:/moodleroot/moodle/plugins/downloaded:ro
    depends_on:
      mariadb:
        condition: service_healthy
      redis:
        condition: service_healthy
      moodle-init:
        condition: service_completed_successfully

  mariadb:
    image: mariadb:10.11
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    environment:
      MARIADB_ROOT_PASSWORD: rootpassword
      MARIADB_USER: moodle_user
      MARIADB_PASSWORD: moodle_pass
      MARIADB_DATABASE: moodle
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-prootpassword"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    command: redis-server --requirepass "moodledevpass" --maxmemory-policy allkeys-lru
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "moodledevpass", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # plugin-downloader:
  #   image: alpine/git:latest
  #   deploy:
  #     resources:
  #       limits:
  #         cpus: "0.2"
  #         memory: 128M
  #   volumes:
  #     - plugins_downloads:/plugin_downloads
  #     - ./.docker/plugin-downloader/download_plugin.sh:/scripts/download_plugins.sh:ro
  #   environment:
  #     PLUGINS_DIR: "/plugin_downloads"
  #     PLUGINS_TO_DOWNLOAD: >
  #       https://moodle.org/plugins/download.php/36782/theme_seo_moodle50_2025070900.zip,theme_seo;
  #       https://moodle.org/plugins/download.php/36820/mod_minilesson_moodle50_2025071300.zip,mod_minilesson;
  #       https://moodle.org/plugins/download.php/36766/theme_boost_union_moodle50_2025041414.zip,theme_boost_union;
  #       https://moodle.org/plugins/download.php/36807/mod_pdfannotator_moodle50_2025071100.zip,mod_pdfannotator
  #   entrypoint: /bin/sh
  #   command:
  #     - "/scripts/download_plugins.sh"

volumes:
  moodledata_data:
  mariadb_data:
  redis_data:
  # plugins_downloads:
