services:
  moodle-init:
    build: &moodle_build
      context: .
      dockerfile: Dockerfile
    environment: &moodle_env
      SITE_URL: http://localhost:8080
      MOODLE_LANGUAGE: en
      MOODLE_SITENAME: "Local Development Moodle"
      MOODLE_SITESUMMARY: "Moodle development instance powered by Nginx and Redis"
      MOODLE_USERNAME: admin
      MOODLE_PASSWORD: MoodleAdmin123!
      MOODLE_EMAIL: admin@local.moodle
      DB_TYPE: mariadb
      DB_HOST: mariadb
      DB_HOST_PORT: 3306
      DB_NAME: moodle
      DB_USER: moodle_user
      DB_PASS: moodle_pass
      DB_PREFIX: mdl_
      REDIS_SESSION_ID_HOST: redis
      REDIS_SESSION_ID_PORT: 6379
      REDIS_SESSION_ID_AUTH_STRING: "moodledevpass"
      REDIS_SESSION_IP_AND_PORT: redis:6379
      REDIS_SESSION_AUTH_STRING: "moodledevpass"
      REDIS_APP_IP_AND_PORT: redis:6379
      REDIS_APP_AUTH_STRING: "moodledevpass"
      REDIS_LOCK_HOST_AND_PORT: redis:6379
      REDIS_LOCK_AUTH_STRING: "moodledevpass"
      ENABLE_FRESHCLAM: "no"
      ENABLE_MOOSH_BOOTSTRAP: "no"
      ENABLE_OBJECTFS: "yes"
    entrypoint:
      - /opt/setup_moodle.sh
    volumes:
      - moodledata_data:/moodleroot/moodledata
    depends_on:
      mariadb:
        condition: service_healthy
      redis:
        condition: service_healthy
      # plugin-downloader:
      #   condition: service_completed_successfully

  moodle:
    build: *moodle_build
    entrypoint:
      - /bin/sh
      - -c
      - |
        . /opt/setup_moodle.sh
        if [ "$${ENABLE_OBJECTFS:-no}" = "yes" ] && ! grep -q "alternative_file_system_class" /moodleroot/moodle/config.php; then
          echo "Injecting ObjectFS into config.php from Compose..."
          sed -i "/require_once/i \$$CFG->alternative_file_system_class = '\\\\tool_objectfs\\\\s3_file_system';" /moodleroot/moodle/config.php
          sed -i "/require_once/i \$$CFG->disablebyteserving = true;" /moodleroot/moodle/config.php
          sed -i "/require_once/i unset(\$$CFG->xsendfilealiases);" /moodleroot/moodle/config.php
          sed -i "/require_once/i \$$CFG->xsendfile = null;" /moodleroot/moodle/config.php
        fi
        /usr/sbin/crond
        /usr/sbin/php-fpm82
        /usr/sbin/nginx -g 'daemon off;'
    ports:
      - "8080:8080"
    environment:
      <<: *moodle_env
      ENABLE_OBJECTFS: "yes"
      OBJECTFS_S3_KEY: minioadmin
      OBJECTFS_S3_SECRET: minioadmin
      OBJECTFS_S3_BUCKET: moodle
      OBJECTFS_S3_REGION: us-east-1
      OBJECTFS_S3_BASE_URL: http://minio.local:9000
      OBJECTFS_KEY_PREFIX: objectfs/
      OBJECTFS_BUCKET_ACL: private
      OBJECTFS_ENABLE_TASKS: "yes"
      OBJECTFS_PREFER_EXTERNAL: "no"
      OBJECTFS_SIZE_THRESHOLD: 0
      OBJECTFS_MINIMUM_AGE: 0
      OBJECTFS_DELETE_LOCAL: "yes"
      OBJECTFS_CONSISTENCY_DELAY: 60
      OBJECTFS_DELETE_EXTERNAL: 2
      OBJECTFS_ENABLE_PRESIGNED_URLS: "no"
      OBJECTFS_PRESIGNED_MIN_FILE_SIZE: 0
      OBJECTFS_PRESIGNED_EXPIRATION: 86400
      OBJECTFS_PRESIGNED_WHITELIST: ""
      OBJECTFS_SIGNING_METHOD: s3
    volumes:
      - moodledata_data:/moodleroot/moodledata
      - objectfs_plugin:/moodleroot/moodle/admin/tool/objectfs:ro
      # - plugins_downloads:/moodleroot/moodle/plugins/downloaded:ro
    depends_on:
      mariadb:
        condition: service_healthy
      redis:
        condition: service_healthy
      moodle-init:
        condition: service_completed_successfully
      minio:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
      objectfs-downloader:
        condition: service_completed_successfully
      objectfs-init:
        condition: service_completed_successfully
      # backup-restorer:
      #   condition: service_completed_successfully

  mariadb:
    image: mariadb:10.11
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    environment:
      MARIADB_ROOT_PASSWORD: rootpassword
      MARIADB_USER: moodle_user
      MARIADB_PASSWORD: moodle_pass
      MARIADB_DATABASE: moodle
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-prootpassword"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    command: redis-server --requirepass "moodledevpass" --maxmemory-policy allkeys-lru
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "moodledevpass", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9090"
    ports:
      - "19000:9000"
      - "19090:9090"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
      MINIO_DOMAIN: minio.local
    volumes:
      - minio_data:/data
    networks:
      default:
        aliases:
          - minio.local
          - moodle.minio.local
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 30s
      timeout: 10s
      retries: 5

  minio-init:
    image: minio/mc:latest
    volumes:
      - ./.docker/minio/policy.json:/tmp/policy.json:ro
    entrypoint: /bin/sh
    command:
      - -c
      - |-
        set -e
        mc alias set bucketer http://minio:9000 minioadmin minioadmin
        mc mb bucketer/moodle --ignore-existing
        mc anonymous set-json /tmp/policy.json bucketer/moodle
    depends_on:
      minio:
        condition: service_healthy

  objectfs-downloader:
    image: alpine/git:latest
    volumes:
      - objectfs_plugin:/plugin_downloads
      - ./.docker/plugin-downloader/download_tool_objectfs.sh:/scripts/download_tool_objectfs.sh:ro
    environment:
      PLUGIN_DIR: /plugin_downloads
      PLUGIN_REPO: https://github.com/catalyst/moodle-tool_objectfs.git
      PLUGIN_BRANCH: MOODLE_404_STABLE
    entrypoint: /bin/sh
    command:
      - /scripts/download_tool_objectfs.sh

  objectfs-init:
    build: *moodle_build
    entrypoint:
      - /bin/sh
      - -c
      - |
        . /opt/setup_moodle.sh
        sh /scripts/objectfs_init.sh
    environment:
      <<: *moodle_env
      ENABLE_OBJECTFS: "yes"
      OBJECTFS_S3_KEY: minioadmin
      OBJECTFS_S3_SECRET: minioadmin
      OBJECTFS_S3_BUCKET: moodle
      OBJECTFS_S3_REGION: us-east-1
      OBJECTFS_S3_BASE_URL: http://minio.local:9000
      OBJECTFS_KEY_PREFIX: objectfs/
      OBJECTFS_BUCKET_ACL: private
      OBJECTFS_ENABLE_TASKS: "yes"
      OBJECTFS_PREFER_EXTERNAL: "no"
      OBJECTFS_SIZE_THRESHOLD: 0
      OBJECTFS_MINIMUM_AGE: 0
      OBJECTFS_DELETE_LOCAL: "yes"
      OBJECTFS_CONSISTENCY_DELAY: 60
      OBJECTFS_DELETE_EXTERNAL: 2
      OBJECTFS_ENABLE_PRESIGNED_URLS: "no"
      OBJECTFS_PRESIGNED_MIN_FILE_SIZE: 0
      OBJECTFS_PRESIGNED_EXPIRATION: 86400
      OBJECTFS_PRESIGNED_WHITELIST: ""
      OBJECTFS_SIGNING_METHOD: s3
    volumes:
      - moodledata_data:/moodleroot/moodledata
      - objectfs_plugin:/moodleroot/moodle/admin/tool/objectfs:ro
      - ./.docker/objectfs/objectfs_init.sh:/scripts/objectfs_init.sh:ro
      - ./.docker/objectfs/configure_objectfs.php:/scripts/configure_objectfs.php:ro
    depends_on:
      mariadb:
        condition: service_healthy
      redis:
        condition: service_healthy
      moodle-init:
        condition: service_completed_successfully
      minio:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
      objectfs-downloader:
        condition: service_completed_successfully

  # backup-restorer:
  #   build: *moodle_build
  #   image: moodle-nginx:latest
  #   entrypoint:
  #     - /bin/sh
  #     - /scripts/restore_course_backups.sh
  #   environment:
  #     <<: *moodle_env
  #     MOODLE_BACKUP_IMPORT_DIR: /moodleroot/backups
  #     MOODLE_BACKUP_RESTORE_CATEGORY_ID: 1
  #   volumes:
  #     - moodledata_data:/moodleroot/moodledata
  #     - moodle_config_data:/shared-config
  #     - ./backups:/moodleroot/backups:ro
  #     - ./.docker/backups/restore_course_backups.sh:/scripts/restore_course_backups.sh:ro
  #   depends_on:
  #     mariadb:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #     moodle-init:
  #       condition: service_completed_successfully
  #     objectfs-init:
  #       condition: service_completed_successfully

  # plugin-downloader:
  #   image: alpine/git:latest
  #   deploy:
  #     resources:
  #       limits:
  #         cpus: "0.2"
  #         memory: 128M
  #   volumes:
  #     - plugins_downloads:/plugin_downloads
  #     - ./.docker/plugin-downloader/download_plugin.sh:/scripts/download_plugins.sh:ro
  #   environment:
  #     PLUGINS_DIR: "/plugin_downloads"
  #     PLUGINS_TO_DOWNLOAD: >
  #       https://moodle.org/plugins/download.php/36782/theme_seo_moodle50_2025070900.zip,theme_seo;
  #       https://moodle.org/plugins/download.php/36820/mod_minilesson_moodle50_2025071300.zip,mod_minilesson;
  #       https://moodle.org/plugins/download.php/36766/theme_boost_union_moodle50_2025041414.zip,theme_boost_union;
  #       https://moodle.org/plugins/download.php/36807/mod_pdfannotator_moodle50_2025071100.zip,mod_pdfannotator
  #   entrypoint: /bin/sh
  #   command:
  #     - "/scripts/download_plugins.sh"

volumes:
  moodledata_data:
  mariadb_data:
  redis_data:
  minio_data:
  objectfs_plugin:
  # plugins_downloads:
